# Запрещаем прямой доступ к папке из браузера (Apache).
# Работает только если .htaccess вообще обрабатывается веб-сервером —
# см. docs/DEPLOY.md про правильную настройку document root.
Require all denied
